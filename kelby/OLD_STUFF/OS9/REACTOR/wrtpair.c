#include <stdio.h>#include <math.h>#include <time.h>int sw1,sw2,sw3,sw4,sw5,sw6,sw7,sw8;short fdata[2][256],ipk[2],pk[2];double pd[2],dpd[2];struct{unsigned int trig;       unsigned int reset;       unsigned int status;       unsigned int sample;       unsigned int delay;       unsigned int rdall;       unsigned int rd2;       unsigned int rd1;       unsigned int rd0;}fermi;unsigned short *reg;unsigned short *reg0,*reg1,*reg2;unsigned int base=0xf0ee0000;unsigned int base2=0x80ee0000;    int event,evwrt,evpair,record;main(argc,argv,envs)int argc;char *argv[];char *envs[];  { FILE *fp;    double asum,axsum,axxsum,ped,dped,sig2,aval;    int timc,timf,i,icap,l;    int zone,sector,card,time1,time2,time3,time4;    int k,ierr,nwds;    int imax0,max0,imax1,max1,dt0,dt1;    int k1,k2,deldt;    double capacitor,dacval;    double charge,charge_true;    char xstring[80],ystring[80],string[80],err[80];    unsigned short cadr,tc,tf;    unsigned short sval,dval;    int kk;    kk=0;    event=0;    evwrt=0;    evpair=0;    fp=fopen("wrtpair.data","w");    fermi_setup();    zone = 1;    sector = 1;    time1 = 0;    time2 = 0;    time3 = 0;    time4 = 0;    set_drawer_timer(zone,sector,1,time1);    set_drawer_timer(zone,sector,2,time2);    set_drawer_timer(zone,sector,3,time3);    set_drawer_timer(zone,sector,4,time4);    multi_low();    for(i=0;i<2;i++)       { card = 45;         if(i==1) card=47;         l = (zone<<12) | (sector<<6) | card;         cadr = (unsigned short)l;         card_sel(cadr);         send13_3in1(2,0);   /* s1=s2=s3=s4=0 */         send4_3in1(1,0);   /* itr=0 */         send4_3in1(0,1);   /* ire=0 */         send4_3in1(3,1);   /* mse=0 */         send4_3in1(4,0);   /* small C disable */         send4_3in1(5,0);   /* large C disable */         send4_3in1(7,0);   /* trig disable */       }         fermi_setup();loop:        fermi_reset();        rlatch();wait:        tsleep(0x80000010);        k=latch();        if(k == 0) goto wait;/*     go read the fermi module ------------------------------- */        fermi_read();/* =========== tube 1 ped etc ======================= */       asum = 0.0;       axsum = 0.0;       axxsum = 0.0;       for(kk=2;kk<7;kk++)        { aval = (double)fdata[0][kk];          asum = asum+1.0;          axsum = axsum + aval;          axxsum = axxsum + aval*aval;        }       ped = axsum / asum;       dped = 0.0;       sig2=axxsum/asum - ped*ped;       if(sig2>0.0) dped=sqrt(sig2);       pd[0] = ped;       dpd[0] = dped;       k1 = ipk[0]-3;       k2 = ipk[0]+3;/* secondary peak? */       max0=0;       imax0=0;       for(kk=0;kk<256;kk++)        { if(kk>ipk[0]-3 && kk<ipk[0]+5) continue;          if(fdata[0][kk]>max0)             { max0= fdata[0][kk];              imax0 = kk;             }        }       if(fdata[0][imax0]<pd[0]+25.0*dpd[0]) imax0=0;/* =========== tube 2 ped etc ======================= */       asum = 0.0;       axsum = 0.0;       axxsum = 0.0;       for(kk=2;kk<7;kk++)        { aval = (double)fdata[1][kk];          asum = asum+1.0;          axsum = axsum + aval;          axxsum = axxsum + aval*aval;        }       ped = axsum / asum;       dped = 0.0;       sig2=axxsum/asum - ped*ped;       if(sig2>0.0) dped=sqrt(sig2);       pd[1] = ped;       dpd[1] = dped;       k1 = ipk[1]-3;       k2 = ipk[1]+3;       max1=0;       imax1 = 0;       for(kk=0;kk<256;kk++)        { if(kk>ipk[1]-3 && kk<ipk[1]+5) continue;          if(fdata[1][kk]>max1)             { max1= fdata[1][kk];              imax1 = kk;             }        }       if(fdata[1][imax1]<pd[1]+25.0*dpd[1]) imax1=0;/* ==================== display ================================= */   sw1 = sw(1);   dt0=0;   if(max0>150)     { dt0 = imax0-ipk[0];      if(dt0<0) dt0=-dt0;      if(sw(3) == 1) sw1 = 1;      printf(" ipk0=%d  imax0=%d  dt0=%d\n",          ipk[0],imax0,dt0);    }   dt1=0;   if(max1>150)    { dt1 = imax1-ipk[1];      if(dt1<0) dt1=-dt1;      if(sw(3) == 1) sw1 = 1;      printf(" ipk1=%d  imax1=%d  dt1=%d\n",          ipk[1],imax1,dt1);    }   deldt=dt0-dt1;   if(deldt<0) deldt=-deldt;   record=0;   if( (imax0 > 20) && (fdata[0][imax0]>100) ) record=1;   if( (imax1 > 20) && (fdata[1][imax1]>100) ) record=2;   if(ipk[0]>20) record=3;   if(ipk[1]>20) record=4;   if(record!=0 || sw(7)==1)      { nwds=fwrite(&fdata[0][0],2,516,fp);       ierr = ferror(fp);       if(ierr != 0) printf("fwrite error=%d  nwds=%d\n",ierr,nwds);       evwrt++;       if(record |= 0) evpair++;       if(sw(3) == 1)          printf(" event=%d  evwrt=%d  evpair=%d  record=%d  %d %d %d %d\n",          event,evwrt,evpair,record,imax0,imax1,ipk[0],ipk[1]);       if(sw(3) == 1) evdisp(250);     }   if(sw(1) == 1) evdisp(250);   event++;   kk=event/100;   if(100*kk==event) printf(" event=%d  evwrt=%d  evpair=%d\n",         event,evwrt,evpair);   if(sw(8) == 0 && evwrt<100000) goto loop;   ierr = fclose(fp);}  /* end of main */fermi_setup() { FPermit(base,0x100,3);   FPermit(base2,0x100,3);   fermi.trig=base+0x26;   fermi.reset=base+0x24;   fermi.status=base+0x22;   fermi.sample=base+0x20;   fermi.delay=base+0x1e;   fermi.rdall=base2+0x18;   fermi.rd2=base+0x14;   fermi.rd1=base+0x12;   fermi.rd0=base+0x10;/* issue reset */   reg=(unsigned short *)fermi.reset;   *reg=0;/* set to full 255 samples */   reg=(unsigned short *)fermi.sample;   *reg=255;/* set delay */   reg=(unsigned short *)fermi.delay;   *reg=30;   reg0=(unsigned short *) fermi.rd0;   reg1=(unsigned short *) fermi.rd1;   reg2=(unsigned short *) fermi.rd2; }fermi_reset() { /* issue reset */   reg=(unsigned short *)fermi.reset;   *reg=0; }fermi_read() { int stat,data_ready,busy,lcnt,i,k0,k1,k2,kmax0,kmax1,kmax2;   int dd0,dd1,dd2;   unsigned short sd0,sd1,sd2;   int nsamp;   nsamp=256;   /* wait for a trigger */   lcnt=0;loop:   lcnt++;   reg = (unsigned short *)fermi.status;   stat = *reg;   data_ready = stat & 4;   busy = stat & 2;   if(lcnt > 1000 ) goto error;   if ( data_ready == 0 ) goto loop;   /* read the data and find peak channel */   k0=0;   kmax0=0;   k1=0;   kmax1=0;   k2=0;   kmax2=0;   for (i=0;i<nsamp;i++)    { sd0 = *reg0;      dd0 = (int)sd0 & 0xfff;     /*  fdata[0][i] = 0xfff - dd0;      if(fdata[0][i]>kmax0)       { kmax0=fdata[0][i];         k0=i;}  */      sd1 = *reg1;      dd1 = (int)sd1 & 0xfff;      fdata[0][i] = 0xfff - dd1;      if(fdata[0][i]>kmax1)       { kmax1=fdata[0][i];         k1=i;}      sd2 = *reg2;      dd2 = (int)sd2 & 0xfff;      fdata[1][i] = 0xfff - dd2;      if(fdata[1][i]>kmax2)       { kmax2=fdata[1][i];         k2=i;}      sw4 = sw(4);      if(sw4 == 1) printf("i=%d  dd1=%d  dd2=$d\n",i,dd1,dd2);     }   ipk[0]=k1;   pk[0]=kmax1;   ipk[1]=k2;   pk[1]=kmax2;   return(0);error:   printf(" fremi read error\n");   ipk[0]=-1;   ipk[1]=-1;}evdisp(nn) int nn; { int max,mmax,idx,idy,ny,linx,ixbas,iybas,ix,iy,iylst,kx,ky,i,k,ii;   int idel,kk;   double dely,y,dy;   char str[5],string[100];   erasm();   max = pk[0];   mmax=5000;   if(max<4000) mmax=4000;   if(max<3000) mmax=3000;   if(max<2000) mmax=2000;   if(max<1000) mmax=1000;   if(max<800) mmax=800;   if(max<600) mmax=600;   if(max<500) mmax=500;   if(max<400) mmax=400;   if(max<200) mmax=200;   if(max<100) mmax=100;   if(max<50) mmax=50;   idx = 1600/nn;   linx = idx*nn;   ixbas = 100+(2200-linx)/2;   dy = 1000.0/(double)mmax;   iybas = 1900;   mova(ixbas,iybas);   drwr(linx,0);   mova(ixbas,iybas);   iylst = 0;   for(i=0;i<nn;i++)    { ii = fdata[0][i];      y = (double)ii;      y = y*dy;      iy = (int)y;      idy = iy-iylst;      drwr(0,idy);      drwr(idx,0);      iylst=iy;    }      drwr(0,-iylst);/*  label horizontal axis */    idel=10;    if(nn>100) idel=25;    if(nn>200) idel=50;    for(i=0;i<nn+1;i=i+idel)     { ix=ixbas+idx*i;       mova(ix,iybas);       drwr(0,-30);       k=i/2;       if(k*2 == i)         { sprintf(str,"%3d",i);           kx = ix-45;           if(i<100) kx = ix-55;           if(i<10) kx=ix-75;           symb(kx,iybas-100,0.0,str);         }     }/*  label vertical axis  */    ny=mmax/100;    if(ny>10) ny=10;    if(mmax == 200) ny=4;    if(mmax == 100) ny=4;    if(mmax == 50) ny=5;    y=(double)mmax*dy;    iy = (int)y;    mova(ixbas,iybas);    drwr(0,iy);    for(i=0;i<=ny;i++)     { ky = i*mmax/ny;       y = (double)ky * dy;       iy = (int)y + iybas;       mova(ixbas,iy);       drwr(-20,0);       sprintf(str,"%4d",ky);       kx = ixbas-150;       symb(kx,iy-20,0.0,str);     }   sprintf(string,     " tube1  ipk=%d   pk=%d  ped=%7.2f +/- %5.2f  event=%d",       ipk[0],pk[0],pd[0],dpd[0],event);   symb(500,3000,0.0,string);   max = pk[1];   mmax=5000;   if(max<4000) mmax=4000;   if(max<3000) mmax=3000;   if(max<2000) mmax=2000;   if(max<1000) mmax=1000;   if(max<800) mmax=800;   if(max<600) mmax=600;   if(max<500) mmax=500;   if(max<400) mmax=400;   if(max<200) mmax=200;   if(max<100) mmax=100;   if(max<50) mmax=50;   idx = 1600/nn;   linx = idx*nn;   ixbas = 100+(2200-linx)/2;   dy = 1200.0/(double)mmax;   iybas = 300;   mova(ixbas,iybas);   drwr(linx,0);   mova(ixbas,iybas);   iylst = 0;   for(i=0;i<nn;i++)    { ii = fdata[1][i];      y = (double)ii;      y = y*dy;      iy = (int)y;      idy = iy-iylst;      drwr(0,idy);      drwr(idx,0);      iylst=iy;    }      drwr(0,-iylst);/*  label horizontal axis */    idel=10;    if(nn>100) idel=25;    if(nn>200) idel=50;    for(i=0;i<nn+1;i=i+idel)     { ix=ixbas+idx*i;       mova(ix,iybas);       drwr(0,-30);       k=i/2;       if(k*2 == i)         { sprintf(str,"%3d",i);           kx = ix-45;           if(i<100) kx = ix-55;           if(i<10) kx=ix-75;           symb(kx,iybas-100,0.0,str);         }     }/*  label vertical axis  */    ny=mmax/100;    if(ny>10) ny=10;    if(mmax == 200) ny=4;    if(mmax == 100) ny=4;    if(mmax == 50) ny=5;    y=(double)mmax*dy;    iy = (int)y;    mova(ixbas,iybas);    drwr(0,iy);    for(i=0;i<=ny;i++)     { ky = i*mmax/ny;       y = (double)ky * dy;       iy = (int)y + iybas;       mova(ixbas,iy);       drwr(-20,0);       sprintf(str,"%4d",ky);       kx = ixbas-150;       symb(kx,iy-20,0.0,str);     }   sprintf(string,     " tube2  ipk=%d   pk=%d  ped=%7.2f +/- %5.2f  event=%d",       ipk[1],pk[1],pd[1],dpd[1],event);   symb(500,1600,0.0,string);    plotmx(); }sw(i)   /* new switch timer module */int i;{ int k,l,shift;  unsigned short *adr;  adr = (unsigned short *)( 0xf0aa000c);  k = *adr;  shift = i-1;  l = (k>>shift) & 1;  return(l);}latch(){ int k,l;  unsigned short *adr;  (unsigned short *)adr = (unsigned short *)0xf0aa000c;  k = *adr;  l = (k>>15) & 1;  return(l);}rlatch(){unsigned short *adr;  (unsigned short *)adr = (unsigned short *)0xf0aa0002; *adr = 0;              /* reset the latch bit */}