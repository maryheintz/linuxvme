#include <stdio.h>#define MAXCHARS 60getline(s)char s[];{  int c,i,lim;  lim=MAXCHARS;  i=0;  while(--lim>0&&(c=getchar()) != EOF && c != '\n')    s[i++] =c;  s[i]='\0';  if(i<1)    i=EOF;  return(i);}/*  Same thing, but for input from a file   */fgetline(fp,s)FILE *fp;char s[];{  int c,i,lim;  lim=MAXCHARS;  i=0;  while(--lim>0&&(c=getc(fp)) != EOF && c != '\n')    s[i++] =c;  s[i]='\0';  if(i<1)    i=EOF;  return(i);}