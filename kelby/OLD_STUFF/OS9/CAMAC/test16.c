#include <stdio.h>main()  { unsigned long ext,b,c,n,a,f,q,off;    unsigned short data;    b=0;    c=1;    n=2;    a=0;/*  set up control word ext */    cdreg(&ext,&b,&c,&n,&a);    printf(" ext=%x\n",ext);    fflush(stdout);    off=0;    printf(" calling cccz \n");    fflush(stdout);    cccz(&ext);   /* initialize */     printf(" calling cccc \n");    fflush(stdout);    cccc(&ext);  /* clear */    printf(" calling ccci \n");    fflush(stdout);    ccci(&ext,&off);  /*  remove crate inhibit *//*  set data register */loop:/*  read switches     a=1;    n=7;    f=0;    cdreg(&ext,&b,&c,&n,&a);    cssa(&f,&ext,&data,&q);    write to data register     a=0;    f=16;    cssa(&f,&ext,&data,&q);  *//*  write to pulser */    a=7;    n=2;    f=16;    data=0xaaaa;    cdreg(&ext,&b,&c,&n,&a);    cssa(&f,&ext,&data,&q);    data=0x5555;    cssa(&f,&ext,&data,&q);    if(f!=100000) goto loop;    }