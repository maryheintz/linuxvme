#include <stdio.h>main()  {     char string[80];    unsigned char gpib_device=1;    char *label;    double ang;    int target;    gpib_reset();    printf("input angle desired\n");    fscanf(stdin,"%lf",&ang);    printf("angle=%f\n",ang);    target=(int)(-ang*100.0);    sprintf(string,"EP1:TA%d:GA\r",target);    sendmessage(gpib_device,string);    sendmessage(gpib_device,"RP1\r");    }