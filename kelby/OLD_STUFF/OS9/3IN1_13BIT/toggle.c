#include <stdio.h>#include <math.h>char string[1000];unsigned char gpib_adr = 3;int ent=0;main(){    int i;   int card,zone,sector,tube,l;   unsigned short cadr;restart:   zone=1;   sector=1;   card=38;   printf(" toggling DG611 switches on card 38 every 2 sec\n");   fflush(stdout);   l = (zone<<12) | (sector<<6) | card;   cadr = (unsigned short)l;   card_sel(cadr);   multi_low();   send4_3in1(4,1);      /* enable small C */   send4_3in1(5,1);      /* enable large C */loop27:       send13_3in1(0,0);       send13_3in1(1,0);       send13_3in1(2,0);       tp_low();       sleep(2);       send13_3in1(0,1);       send13_3in1(1,1);       send13_3in1(2,0xf);       tp_high();       sleep(2);       if(sw(1) == 0) goto loop27;       if(sw(1) == 1) goto restart;}sw(i)   /* new switch timer module */int i;{ int k,l,shift;  unsigned short *adr;  adr = (unsigned short *)( 0xf0aa000c);  k = *adr;  shift = i-1;  l = (k>>shift) & 1;  return(l);}