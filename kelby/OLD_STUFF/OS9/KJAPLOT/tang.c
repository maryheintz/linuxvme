#include <stdio.h>#include <math.h>#include <strings.h>      main()      { int i;        double x,y,del;        char xstring[80],ystring[80];        dotset(0,10000.0,0.0,10.0,0.0,5,5);        del = 0.001;        for(x=0.1;x<10000.0;x=x+del)         { y = 0.47/sqrt(x) + 0.2;           dotacc(0,x,y);           if(x>1.0) del=0.01;           if(x>10.0) del = 0.1;           if(x>100.0) del = 1.0;           if(x>1000.0) del = 10;         }        del = 0.001;        for(x=0.1;x<10000.0;x=x+del)         { y = 2000.0/(1024.0*x);           if(x<33.0) y = 2000.0/(65536.0*x);           dotacc(0,x,y);           if(x>1.0) del=0.01;           if(x>10.0) del = 0.1;           if(x>100.0) del = 1.0;           if(x>1000.0) del = 10;         }        dotwrt(0);       }